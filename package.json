{
  "name": "docseal",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter server start:dev & pnpm --filter web dev",
    "build": "pnpm --filter server build && pnpm --filter web build",
    "lint": "pnpm --filter server lint && pnpm --filter web lint",
    "format": "pnpm --filter server format && pnpm --filter web format"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@nestjs/core",
      "better-sqlite3",
      "esbuild",
      "unrs-resolver",
      "vue-demi"
    ]
  }
}
